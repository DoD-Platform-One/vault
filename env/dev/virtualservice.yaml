apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: vault
  namespace: vault
spec:
  gateways:
  - main.istio-system.svc.cluster.local
  hosts:
  - vault.cnap.dso.mil
  http:
  - route:
    - destination:
        host: vault-ui
        port:
          number: 8200